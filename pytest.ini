[pytest]
addopts = -v --html=report.html --self-contained-html
testpaths = tests
markers =
    smoke: fast critical-path tests, run before every deployment
    regression: full suite, run before every release
